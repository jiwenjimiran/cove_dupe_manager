{
  "name": "cove-duplicate-manager-ui",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.8"
  }
}
